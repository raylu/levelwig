{% extends 'base.jinja2' %}

{% block body %}
	<article>
		<h2>{{ post.title }}</h2>
		on {{ post.date }} by {{ post.username }}
		<blockquote>{{ post.body|markdown }}</blockquote>
	</article>

	<p>
		<a href="/">back</a>
	</p>
{% endblock body %}
