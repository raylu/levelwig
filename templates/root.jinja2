{% extends 'base.jinja2' %}

{% block body %}
	{% for post in posts %}
		<article>
			<h2><a href="/post/{{ post.id }}/{{ post.title|slug }}">{{ post.title }}</a></h2>
			by {{ post.username }}
			<blockquote>{{ post.body }}</blockquote>
		</article>
	{% endfor %}
{% endblock body %}
